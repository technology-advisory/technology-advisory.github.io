name: Actualizar Inteligencia CVE

on:
  schedule:
    - cron: '0 0 * * *' # Ejecución diaria a las 00:00 UTC
  workflow_dispatch:   # Botón manual activo

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Instalar dependencias
        run: pip install requests

      # Ejecutamos ambos scripts uno tras otro
      - name: Ejecutar scripts de actualización (CISA + NVD)
        run: |
          python scripts/update_cves.py
          python scripts/update_nvd.py

      - name: Guardar cambios en el repositorio
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # IMPORTANTE: Añadimos ambos archivos JSON
          git add data/cve.json data/nvd_scores.json
          
          git commit -m "🤖 Actualización automática: CISA KEV y NVD Scores" || echo "No hay cambios para actualizar"
          git push